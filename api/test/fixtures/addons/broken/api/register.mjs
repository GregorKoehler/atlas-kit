/* A fixture addon that cannot load. The framework must record it and carry on:
 * losing the dashboard because an optional feature has a typo is the wrong trade. */
throw new Error('deliberately broken fixture addon')
